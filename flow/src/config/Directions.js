/**
 * Direcciones de flujo soportadas
 * por el generador de diagramas.
 *
 * Los valores corresponden a la
 * sintaxis utilizada por Mermaid.
 */
export const DIRECTIONS = {

    /**
     * Flujo vertical
     * (Top -> Bottom)
     */
    VERTICAL: "TB",

    /**
     * Flujo horizontal
     * (Left -> Right)
     */
    HORIZONTAL: "LR"

};


/**
 * Dirección utilizada por defecto
 * al generar un diagrama.
 */
export const DEFAULT_DIRECTION =
    DIRECTIONS.VERTICAL;